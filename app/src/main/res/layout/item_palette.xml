<?xml version="1.0" encoding="utf-8"?>
<!-- karta wyswietlajaca palete kolorow -->
<androidx.cardview.widget.CardView 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/paletteCard"
    style="@style/AppCardView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/card_margin">

    <!-- kontener z zawartoscia karty -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/padding_medium">

        <!-- nazwa palety -->
        <TextView
            android:id="@+id/paletteName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@color/text_primary"
            android:textSize="@dimen/text_size_large"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="My Palette" />

        <!-- informacje o palecie liczba kolorow i data -->
        <TextView
            android:id="@+id/paletteInfo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_tiny"
            android:textColor="@color/text_secondary"
            android:textSize="@dimen/text_size_small"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/paletteName"
            tools:text="5 colors • 01/01/2024 12:00" />

        <!-- kontener na sloty kolorow -->
        <LinearLayout
            android:id="@+id/colorSlotsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_medium"
            android:orientation="horizontal"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/paletteInfo">

            <!-- slot na pierwszy kolor -->
            <View
                android:id="@+id/colorSlot1"
                android:layout_width="0dp"
                android:layout_height="@dimen/color_slot_size"
                android:layout_weight="1"
                android:layout_marginEnd="2dp"
                tools:background="@color/primary" />

            <!-- slot na drugi kolor -->
            <View
                android:id="@+id/colorSlot2"
                android:layout_width="0dp"
                android:layout_height="@dimen/color_slot_size"
                android:layout_weight="1"
                android:layout_marginEnd="2dp"
                tools:background="@color/accent" />

            <!-- slot na trzeci kolor -->
            <View
                android:id="@+id/colorSlot3"
                android:layout_width="0dp"
                android:layout_height="@dimen/color_slot_size"
                android:layout_weight="1"
                android:layout_marginEnd="2dp"
                tools:background="@color/teal_200" />

            <!-- slot na czwarty kolor -->
            <View
                android:id="@+id/colorSlot4"
                android:layout_width="0dp"
                android:layout_height="@dimen/color_slot_size"
                android:layout_weight="1"
                android:layout_marginEnd="2dp"
                tools:background="@color/purple_500" />

            <!-- slot na piaty kolor -->
            <View
                android:id="@+id/colorSlot5"
                android:layout_width="0dp"
                android:layout_height="@dimen/color_slot_size"
                android:layout_weight="1"
                tools:background="@color/teal_700" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>
